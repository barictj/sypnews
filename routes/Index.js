const ContentRoutes = require('./ContentRoutes')
const TagsRoutes = require('./TagsRoutes')

module.exports = ContentRoutes, TagsRoutes

module.exports = {
    ContentRoutes: ContentRoutes, // or whatever you want to assign it to
    TagsRoutes: TagsRoutes // again, set it to what you like
};

